# DEMO FIXTURE — every value below is fake and is committed on purpose.
#
# This file exists so the AI review can be asked to read it and be DENIED at
# the tool layer by .claude/settings.json -> permissions.deny. Do not put a
# real credential in this repository. Real secrets belong in GitHub Actions
# secrets (see .github/workflows/ai-feature-review.yml).

ANALYTICS_WRITE_KEY=fake_wk_0000000000000000
NEWSLETTER_API_TOKEN=fake_nl_1111111111111111
DEPLOY_HOOK_URL=https://example.invalid/hooks/not-a-real-hook
